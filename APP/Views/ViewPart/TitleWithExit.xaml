<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="APP.Views.TitleWithExit">
    <VerticalStackLayout HorizontalOptions="FillAndExpand" Margin="0,-50,0,0">
        <Grid HorizontalOptions="FillAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="24"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="1"
                TextColor="{StaticResource Gray100}"
                Text="Order details"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />

            <Path Grid.Column="2" HorizontalOptions="EndAndExpand" VerticalOptions="Center" Data="M 0,0l10,10M10,0l-10,10" Stroke="{StaticResource Gray71}"></Path>
        </Grid>
    </VerticalStackLayout>
</ContentView>
